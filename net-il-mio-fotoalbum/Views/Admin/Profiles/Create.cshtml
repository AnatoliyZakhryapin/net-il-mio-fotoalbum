@using Microsoft.AspNetCore.Components

@{
    ViewData["Title"] = "Create Profile";
    Layout = "_DashboardLayout";
}

@model FormModelProfile

<section class="section-categories_create p-4">

    @* Title *@
    <div class="container mb-5">
        <div class="row justify-content-center">
            <div class="col-auto">
                <h1>Add new Profile</h1>
            </div>
        </div>
    </div>

    @* INPUT *@
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-4">
                <form asp-controller="Profile" asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div class="form-group mb-3">
                        <label asp-for="Profile.Name" class="form-label">Name</label>
                        <input asp-for="Profile.Name" type="text" class="form-control" id="name" placeholder="Insert name of Profile">
                        <span asp-validation-for="Profile.Name" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Profile.Surname" class="form-label">Surname</label>
                        <input asp-for="Profile.Surname" type="text" class="form-control" id="name" placeholder="Insert Surname of Profile">
                        <span asp-validation-for="Profile.Surname" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn-custom btn-violet">Add</button>
                </form>
            </div>
        </div>
    </div>
</section>
